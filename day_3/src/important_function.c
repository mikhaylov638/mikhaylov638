#include <stdio.h>
#include <math.h>

/* ВСТАВЬ СЮДА ФОРМУЛУ.
   Пример: если формула f(x) = sin(x) + x*x, то верни sin(x) + x*x.
   Ниже — заглушка 0.0, её нужно заменить своей формулой. */
double f(double x) {
    return 0.0;  // TODO: заменить на настоящую формулу
}

int main(void) {
    double x;

    // Читаем одно число с плавающей точкой; если ввели не число — печатаем n/a
    if (scanf("%lf", &x) != 1) {
        printf("n/a\n");
        return 0;
    }

    double y = f(x);

    // Округление до 1 знака после запятой — формат %.1f сам округлит правильно
    printf("%.1f\n", y);
    return 0;
}
